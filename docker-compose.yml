version: "3.2"
services:
  docs:
    build:
      dockerfile: docker/Dockerfile.prod
      context: .
    ports:
      - "5005:3000"
    env_file:
      - .env
    volumes:
      - ./public/uploads:/uploads
      - ./db:/usr/src/app/db
      - ./app-config.yaml:/usr/src/app/app-config.yaml
